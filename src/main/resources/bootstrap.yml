spring:
  cloud:
    config:
      label: produce
      discovery:
        enabled: true
        service-id: configsever
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
